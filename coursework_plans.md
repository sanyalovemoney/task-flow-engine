📈 Plan No. 3: "High-Performance Big Data Streaming and Analysis Pipeline"1. Thesis Topic (Academic Title)“Research and Development of a System for Streaming Filtration, Transformation, and Analysis of Large Volumes of Structured Data with Reactive Alerting”2. Scientific and Theoretical Novelty & JustificationThe Backpressure Concept: Investigation of the backpressure mechanism within Node.js Streams. How does the system adapt when the data reading speed exceeds the processing/writing speed?Memory Footprint Efficiency: A comparative analysis between the traditional in-memory array processing approach (loading the entire file into RAM) and the streaming approach (Streams) ensuring $O(1)$ memory consumption.Asynchronous Data Generation: Mathematical workload modeling utilizing generators and iterators with randomized timeouts.3. Architecture and Module IntegrationProcessing gigabyte-scale files without server crashes due to memory exhaustion:Фрагмент кодуgraph TD
    Generator[generator.js: Data Stream / Big File] -->|Data Stream| TransformStream[streams.js: CSV/JSON Transform]
    TransformStream -->|Anomaly Analysis| FilterStream[Filter Module]
    FilterStream -->|Anomaly Detected| Emitter[events.js: Alert Emitter]
    Emitter -->|Notification| TelegramBot[Telegram/Email Alert]
    FilterStream -->|Saving Results| DestFile[(Cleaned Large File)]
4. Required Additions for the Coursework (Thesis):Data Ingestion Script: A script (generator.js) that generates a large CSV file (e.g., 1–2 GB of web server logs or financial transactions).ETL Pipeline: A sequence of streams (streams.js) designed to:Read the file in chunks.Parse the rows.Aggregate metrics and statistics (e.g., transaction volume per minute).Filter suspicious transactions (anomalies).Alerting System: Integration of events.js with a basic notification handler. Once an anomaly is detected (e.g., transaction amount $> 1,000,000$), a reactive event is emitted to send a message via a Telegram bot or output a formatted red "Alert" directly to the terminal.Comparative Experiment: Benchmarking RAM consumption when processing 100MB, 500MB, and 1GB files using the standard fs.readFile approach (which will trigger a V8 Out of Memory crash) versus the optimized fs.createReadStream approach.
